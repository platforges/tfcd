name: Deploy
on:
  workflow_dispatch:
  merge_group:
    types: [ checks_requested ]
permissions:
  id-token: write # configure-aws-credentials
concurrency: deploy-main
jobs:
  build:
    uses: ./.github/workflows/build.yml

  deploy-terraform:
    name: Deploy terraform
    runs-on: ubuntu-latest
    needs: build
    environment: dev
    outputs:
      success: ${{ steps.sanity.outputs.success }}
    steps:
    - uses: actions/checkout@v4
    - uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.10.2
    - uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: arn:aws:iam::097614841487:role/GithubActionsRole
        aws-region: us-east-1
    - name: Terraform apply
      run: |
        terraform init -input=false
        terraform apply -auto-approve -input=false -var 'hash=${{ needs.build.outputs.hash }}'
    - name: Fail workflow on deploy error
      if: failure()
      run: echo "Deployment failed, have to rollback 🔴" && exit 1

  sanity-test:
    runs-on: ubuntu-latest
    needs: deploy-terraform
    outputs:
      success: ${{ steps.sanity.outputs.success }}
    steps:
    - name: Simulate sanity check
      id: sanity
      run: |
        echo "success=true" >> $GITHUB_OUTPUT
        exit 0

  rollback-if-failed:
    name: Rollback & re-apply main
    runs-on: ubuntu-latest
    needs: [ deploy-terraform, sanity-test ]
    if: needs.sanity-test.outputs.success == 'false'
    # or deploy-terraform failed
    environment: dev
    steps:
    - uses: actions/checkout@v4
      with:
        ref: main
    - uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.10.2
    - uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: arn:aws:iam::097614841487:role/GithubActionsRole
        aws-region: us-east-1
    - name: Terraform apply
      run: |
        terraform init -input=false
        terraform apply -input=false -auto-approve -var 'hash=main'
    - name: Exit with error
      run: exit 1 # to not merge PR after this
